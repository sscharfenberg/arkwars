extends ../email_layout

block content
    h2 Hello @#{user.username}, your account has been suspended.
    p Admin @#{admin.username} has suspended your user account.
    p
        strong Reason:
        != ` ${reason}`
    p
        strong Suspended Until:
        != ` ${suspendedUntil}`
